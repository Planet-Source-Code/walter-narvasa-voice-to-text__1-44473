Attribute VB_Name = "ModLUtil"

Option Explicit
Option Base 0

'#If Not _LUTIL_H Then
'#Const _LUTIL_H  = True

'#If __cplusplus Then
#If False Then
'The following is an externally defined global variable
'extern "C" {  #endif
'Public Declare Sub InitLUtil Lib "libname" ( ByRef NO_HDR As void typedef enum {, ByVal Arg1 As WMAP_HDR, ByVal Arg2 As CMAP_HDR, ByVal Arg3 As GRAM_HDR, ByRef mem As LFOF_HDR } LMHdrKind typedef struct lmFileHdrRec   LMFileHdr LMHdrKind ReadLMHeader MemHeap, ByRef src As Source, ByRef filter As IOFilter, ByRef hdr As LMFileHdr, ByRef name As int  n int NumLMHdrFields LMFileHdr hdr char   GetLMHdrStr char, ByRef hdr As LMFileHdr, ByRef name As Boolean ucase Boolean GetLMHdrInt char, ByRef value As Long, ByRef hashSize() As LMFileHdr hdr void UpperCase char  s Boolean BlankString char  s typedef struct _NameHolder{ char  name int aux void  ptr struct _NameHolder  wptr struct _NameHolder  next }NameHolder typedef NameHolder  NameId typedef struct { int hashSize char  description long numAccesses long numTests NameHolder   nameTab } HashTab HashTab  CreateHashTable int, ByRef htab As char  description NameId GetNameId HashTab, ByRef name As String, ByRef base As Boolean insert void PrintHashTabStats HashTab
'ab void usort void, ByVal n As Long, ByVal size As Long, ByRef Arg18 As          int  compar   void, ByRef __cplusplus As void   #ifdef )


#If False Then
'I couldn't handle the following:
'}  #endif
#End If


#End If


